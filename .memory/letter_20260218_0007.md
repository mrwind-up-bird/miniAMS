# Letter to Myself (Session Handoff)

**Date:** 2026-02-18

## 1. Executive Summary
* **Goal:** Build, secure, document, and deploy miniAMS — a multi-tenant agency management SaaS
* **Current Status:** FULLY DEPLOYED AND WORKING. Local dev environment running on Docker. All issues resolved.

## 2. The "Done" List (Context Anchor)

### This Session
* Completed security hardening across all action files (13 tenantId WHERE clause fixes, viewer role blocks, schema max-lengths)
* Fixed `removeInvoiceItem` missing tenantId in `recalculateInvoiceTotals` call (`src/lib/actions/invoices.ts`)
* Fixed `createInvoice` missing serviceId cross-tenant validation (`src/lib/actions/invoices.ts`)
* Fixed `updateInvoice` missing customerId validation (`src/lib/actions/invoices.ts`)
* Added tenantId to update/delete WHERE clauses in `customers.ts`, `projects.ts`, `time-entries.ts`, `services.ts`
* Added viewer role checks to create/update in all action files
* Changed `@@unique([tenantId, email])` to `@@unique([email])` in `prisma/schema.prisma`
* Added max-length constraints to all 6 Zod validation schemas
* Added safe pagination to `getTimeEntries`, capped bulk approve/unapprove at 200
* Created `vercel.json`, `.github/workflows/ci.yml`, `README.md` (7 Mermaid diagrams), `QUICKSTART.md`, `.env.example`
* Created `docker-compose.yml` — PostgreSQL 16 Alpine on port 5433
* Fixed deployment: `AUTH_SECRET` env var (not `NEXTAUTH_SECRET`), `AUTH_URL`, removed legacy `@secret` refs from `vercel.json`
* Fixed `src/middleware.ts` — pass `secret: process.env.AUTH_SECRET` to `getToken()` (Edge runtime doesn't auto-read env)
* Merged PR #1, pushed 7 commits total to main

## 3. The "Pain" Log (CRITICAL)
* **Tried:** `NEXTAUTH_SECRET` as env var name for NextAuth v5
* **Failed:** NextAuth v5 (Auth.js) expects `AUTH_SECRET`. Caused 500 on all auth endpoints.
* **Workaround:** Renamed to `AUTH_SECRET` everywhere

* **Tried:** `vercel.json` with `"AUTH_SECRET": "@auth-secret"` (Vercel Secrets syntax)
* **Failed:** `Error: Environment Variable references Secret which does not exist`
* **Workaround:** Removed `env` block; Vercel reads env vars directly from dashboard

* **Tried:** `getToken({ req })` in Edge middleware without explicit secret
* **Failed:** `MissingSecret` error — Edge runtime doesn't auto-read `AUTH_SECRET` from `process.env`
* **Workaround:** Changed to `getToken({ req, secret: process.env.AUTH_SECRET })` in `src/middleware.ts:32`

* *Note:* Always pass `secret` explicitly to `getToken()` in Edge middleware with NextAuth v5.

## 4. Active Variable State
* Live URL: https://mini-ams.vercel.app
* Vercel env vars: `DATABASE_URL` (Neon auto-added), `AUTH_SECRET`, `AUTH_URL`
* Local dev: `docker compose up -d` → PostgreSQL on port 5433, Next.js on port 4000
* Local `.env`: `DATABASE_URL=postgresql://miniams:miniams-dev@localhost:5433/miniams`, `AUTH_URL=http://localhost:4000`
* Demo login: `demo@miniams.dev` / `password123` (owner, German locale)
* Port 3000 occupied by qrecords-dev, port 3001 by qrecords API — miniAMS uses 4000
* Docker container: `miniams-db` with volume `miniams-pgdata`
* **Database credentials exposed in chat** — Neon password needs rotation

## 5. Immediate Next Steps
1. [ ] Rotate Neon database password (credentials visible in chat history)
2. [ ] Optional: PDF invoice generation (React-PDF or Puppeteer)
3. [ ] Optional: Email sending for invoice delivery
4. [ ] Optional: Dark mode toggle
5. [ ] Optional: Wire up AuditLog model for financial operation logging
6. [ ] Optional: Rate limiting middleware
