# Letter to Myself (Session Handoff)

**Date:** 2026-02-18

## 1. Executive Summary
* **Goal:** Build miniAMS Phase 4 (Invoicing) - full invoice CRUD, service catalog, time entry import, KPI dashboard
* **Current Status:** Phase 4 complete. Build passes. Starting Phase 5 (Dashboard & Settings).

## 2. The "Done" List (Context Anchor)
* `src/lib/validations/invoice.ts` - Zod schemas for invoice create/update/items
* `src/lib/validations/service.ts` - Zod schemas for service CRUD
* `src/lib/actions/invoices.ts` - 12 server actions: CRUD, status transitions, stats, time entry import
* `src/lib/actions/services.ts` - 5 server actions: CRUD for service catalog
* `src/components/invoices/invoice-page-client.tsx` - KPI cards + list page
* `src/components/invoices/invoice-list.tsx` - Mobile cards + desktop table with status badges
* `src/components/invoices/invoice-form.tsx` - 2-step sheet form with inline line items
* `src/components/invoices/time-entry-picker.tsx` - Approved time entry selector grouped by project
* `src/components/invoices/invoice-detail-client.tsx` - Full detail with status actions
* `src/app/[locale]/(dashboard)/invoices/page.tsx` - Server page with stats + customer list
* `src/app/[locale]/(dashboard)/invoices/[id]/page.tsx` - Detail server page
* Updated StatusBadge to support `type="invoice"` with 5 invoice statuses
* Updated en.json and de.json with ~50 new invoice translation keys
* Invoice number auto-generation: RE-YYYY-NNNN via InvoiceSequence atomic upsert
* Status workflow: draft -> sent -> paid/overdue -> cancelled (with role gates)
* Time entry integration: import approved entries, mark as invoiced on create, release on cancel

## 3. The "Pain" Log (CRITICAL)
* **Tried:** Write tool for new files without Read first
* **Failed:** "File has not been read yet" error even for new files
* **Workaround:** Read the file first (even if it doesn't exist or was created by an agent), then Write
* **Tried:** Frontend agent for invoice UI
* **Failed:** Agent hit rate limit (different from backend agent which succeeded)
* **Workaround:** Backend agent created most frontend files too; I created the remaining 2 manually

## 4. Active Variable State
* All Decimal fields from Prisma serialized to Number before client
* Invoice stats use `invoiceCount` (Record<InvoiceStatus, number>) from backend, mapped to `counts` in page.tsx
* InvoiceForm uses 2-step flow: Step 1 = customer/dates/currency, Step 2 = line items

## 5. Immediate Next Steps
1. [ ] Phase 5: Dashboard with real data (revenue, open invoices, tracked hours, active projects)
2. [ ] Phase 5: Settings pages (tenant settings, user profile, team management)
3. [ ] Final polish and production readiness
