# Letter to Myself (Session Handoff)

**Date:** 2026-02-18

## 1. Executive Summary
* **Goal:** Build and deploy miniAMS — a multi-tenant agency management SaaS (CRM, Projects, Time Tracking, Invoicing)
* **Current Status:** FULLY DEPLOYED AND WORKING at https://mini-ams.vercel.app

## 2. The "Done" List (Context Anchor)

### Application (All 5 Phases)
* Phase 1: Foundation — Next.js 16, Prisma 6, NextAuth v5, next-intl (DE/EN), shadcn/ui, 13 Prisma models
* Phase 2: CRM — Customers CRUD, contacts inline, projects with team members
* Phase 3: Time Tracking — Timer, manual entries, weekly grid view, approval workflow
* Phase 4: Invoicing — CRUD, atomic RE-YYYY-NNNN numbering, time entry import, status workflow
* Phase 5: Dashboard (real KPIs), Settings (profile, company, team management)

### Security Hardening (12 issues resolved)
* CRITICAL: `requireAuth()` in `src/lib/tenant.ts` checks tenantId
* HIGH: Cross-tenant validation on all foreign key references (invoices, services, time entries)
* MEDIUM: tenantId in all 13 update/delete WHERE clauses, viewer role blocked from writes
* MEDIUM: Global email uniqueness `@@unique([email])`, max-length on all Zod schemas
* Bulk operations capped (approve/unapprove max 200), safe pagination on all list queries

### CI/CD & Documentation
* `vercel.json` — Vercel deployment config (fra1 region)
* `.github/workflows/ci.yml` — GitHub Actions CI (lint, typecheck, build)
* `README.md` — 7 Mermaid diagrams (architecture, ERD, data flow, invoice lifecycle, time entry workflow, RBAC, module overview)
* `QUICKSTART.md` — Setup guide with prerequisites, demo credentials, all commands
* `.env.example` — Template for required env vars

### Production Deployment
* Fixed `AUTH_SECRET` env var (NextAuth v5 uses `AUTH_SECRET`, not `NEXTAUTH_SECRET`)
* Fixed `AUTH_URL` env var (was defaulting to localhost:3000)
* Removed legacy `@secret` references from `vercel.json`
* Database: Neon PostgreSQL (eu-central-1), schema pushed and seeded
* PR #1 merged, all code on `main`

## 3. The "Pain" Log (CRITICAL)
* **Tried:** `NEXTAUTH_SECRET` as the environment variable name for NextAuth v5
* **Failed:** NextAuth v5 (Auth.js) expects `AUTH_SECRET`, not `NEXTAUTH_SECRET`. Caused 500 on `/api/auth/error`
* **Workaround:** Renamed to `AUTH_SECRET` in Vercel env vars and all config files
* **Tried:** `vercel.json` with `"AUTH_SECRET": "@auth-secret"` (Vercel Secrets syntax)
* **Failed:** `Error: Environment Variable references Secret which does not exist`
* **Workaround:** Removed `env` block from `vercel.json`; Vercel reads env vars directly from dashboard
* **Tried:** Deploying without `AUTH_URL`
* **Failed:** Provider URLs showed `localhost:3000`, auth callbacks broken
* **Workaround:** Added `AUTH_URL=https://mini-ams.vercel.app` to Vercel env vars

## 4. Active Variable State
* Vercel project: https://vercel.com/oliver-baers-projects/mini-ams
* Live URL: https://mini-ams.vercel.app
* Database: Neon PostgreSQL `ep-wispy-pine-agex4ywr-pooler.c-2.eu-central-1.aws.neon.tech`
* Vercel env vars: `DATABASE_URL`, `AUTH_SECRET`, `AUTH_URL` + Neon auto-added vars
* Demo login: `demo@miniams.dev` / `password123` (owner role, German locale)
* Git: all on `main` branch, 4 commits total
* **IMPORTANT:** Database credentials were exposed in conversation — rotate Neon password

## 5. Immediate Next Steps
1. [ ] Rotate Neon database password (credentials visible in chat history)
2. [ ] Optional: PDF invoice generation (React-PDF or Puppeteer)
3. [ ] Optional: Email sending (invoice delivery)
4. [ ] Optional: Dark mode toggle
5. [ ] Optional: Audit logging (AuditLog model exists, not yet wired)
6. [ ] Optional: Rate limiting middleware
