# Letter to Myself (Session Handoff)

**Date:** 2026-02-17 ~23:55

## 1. Executive Summary
* **Goal:** Build miniAMS — multi-tenant agency management SaaS with CRM, Projects, Time Tracking, Invoicing modules
* **Current Status:** Phase 2 CRM Customers fully implemented. Projects pages being built by background worker. Build passes clean.

## 2. The "Done" List (Context Anchor)
* **Server Actions** (`src/lib/actions/`):
  - `customers.ts`: getCustomers (search/filter/paginate), getCustomer, createCustomer, updateCustomer, deleteCustomer (soft delete, role-gated)
  - `contacts.ts`: getContacts, createContact, updateContact, deleteContact (verifies customer belongs to tenant)
  - `projects.ts`: getProjects (multi-filter), getProject (with members), createProject, updateProject, deleteProject, addProjectMember, removeProjectMember
* **Zod Validation** (`src/lib/validations/`):
  - `customer.ts`: customerCreateSchema, customerUpdateSchema
  - `contact.ts`: contactCreateSchema, contactUpdateSchema
  - `project.ts`: projectCreateSchema, projectUpdateSchema, projectMemberSchema
* **Shared Components** (`src/components/shared/`):
  - `status-badge.tsx`: Colored badges for customer/project statuses with dark mode support
  - `empty-state.tsx`: Reusable empty state with icon, title, description, ReactNode action
  - `confirm-dialog.tsx`: Confirmation dialog with loading state, destructive variant
  - `search-bar.tsx`: Debounced URL search param integration
* **Customer Pages**:
  - `src/app/[locale]/(dashboard)/customers/page.tsx`: Server component with search params
  - `customer-page-client.tsx`: Mobile cards + desktop table + search + empty state + add sheet
  - `customer-list.tsx`: Responsive list with cards (<md) and table (md+), inline delete with confirmation
  - `customer-form.tsx`: Sheet form with controlled inputs, Zod field errors, currency/status selects
  - `src/app/[locale]/(dashboard)/customers/[id]/page.tsx`: Detail page (server component)
  - `customer-detail-client.tsx`: Header + status badge + edit/delete + tabs (Info, Contacts, Projects, Activity)
  - `customer-info-tab.tsx`: Display fields in cards (contact info, billing info)
  - `contacts-tab.tsx`: Inline CRUD for contacts with Sheet form, primary badge, delete confirmation
  - `customer-projects-tab.tsx`: Project cards linked to project detail pages
* Updated `messages/en.json` and `messages/de.json` with all CRM translation keys

## 3. The "Pain" Log (CRITICAL)
* **Tried:** Prisma 6 `prisma-client` generator with custom output to `src/generated/prisma`
* **Failed:** Turbopack can't resolve `@/generated/prisma` — Module not found
* **Workaround:** Use `prisma-client-js` generator, import from `@prisma/client`
* *Note:* Do NOT use `provider = "prisma-client"` with custom output in this project.

* **Tried:** Adding `seed` property to `prisma.config.ts` defineConfig
* **Failed:** TypeScript error — `seed` not in type `PrismaConfigUnconditional & SchemaEngineConfigClassic`
* **Workaround:** Keep seed config in `package.json#prisma.seed` instead
* *Note:* prisma.config.ts does NOT support seed in Prisma 6.

* **Tried:** EmptyState with `action: {label, onClick}` object interface
* **Failed:** Inconsistent usage — some components passed ReactNode, others passed object
* **Workaround:** Changed EmptyState to accept `action?: React.ReactNode` for flexibility

## 4. Active Variable State
* `.env`: DATABASE_URL=postgresql://postgres:postgres@localhost:5432/miniams, AUTH_SECRET=dev-secret, AUTH_URL=http://localhost:3000
* `package.json#prisma.seed`: "tsx prisma/seed.ts"
* Team `miniams-phase2` active with `projects-dev` worker building project pages in background
* Build passes clean with all CRM routes registered
* 18 shadcn/ui components installed, Zod v4 for validation

## 5. Immediate Next Steps
1. [ ] Complete Projects list + detail pages (worker in progress)
2. [ ] Security review of all CRM server actions (Task #5)
3. [ ] Phase 3: Time Tracking — timer mode, manual entry, weekly view, approval workflow
4. [ ] Phase 4: Invoicing — create from time entries, PDF generation, status workflow
5. [ ] Phase 5: Dashboard with real data, settings pages
6. [ ] Need PostgreSQL running locally to fully test auth + CRUD flows
