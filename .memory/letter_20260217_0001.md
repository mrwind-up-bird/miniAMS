# Letter to Myself (Session Handoff)

**Date:** 2026-02-17 ~23:30

## 1. Executive Summary
* **Goal:** Build miniAMS — a multi-tenant agency management SaaS (CRM + Projects + Time Tracking + Invoicing) targeting small DACH-region agencies.
* **Current Status:** Phase 1 Foundation complete. Build succeeds. All placeholder pages render. Ready for Phase 2 CRM implementation.

## 2. The "Done" List (Context Anchor)
* Initialized Next.js 16 (App Router) + TypeScript + Tailwind CSS v4
* Installed and configured Prisma 6 with full multi-tenant schema (13 models: Tenant, User, AuditLog, Customer, Contact, Project, ProjectMember, TimeEntry, Service, Invoice, InvoiceItem, InvoiceSequence)
* Configured NextAuth v5 with Credentials provider + JWT sessions (`src/lib/auth.ts`)
* Session carries: userId, tenantId, role, locale
* Set up next-intl v4 with DE+EN locale prefix routing (`src/i18n/routing.ts`, `src/i18n/request.ts`, `src/i18n/navigation.ts`)
* Installed 18 shadcn/ui components (Button, Input, Label, Card, Table, Dialog, Sheet, DropdownMenu, Badge, Tabs, Select, Calendar, Popover, Command, Separator, Avatar, Skeleton, Sonner, Tooltip, Textarea, Checkbox)
* Built app shell: collapsible Sidebar (`src/components/layout/sidebar.tsx`), TopBar with locale switcher + user menu (`src/components/layout/top-bar.tsx`), MobileNav bottom tabs (`src/components/layout/mobile-nav.tsx`)
* Created auth pages: Login (`src/app/[locale]/(auth)/login/page.tsx`), Register (`src/app/[locale]/(auth)/register/page.tsx`)
* Registration API creates tenant + user + invoice sequence (`src/app/api/auth/register/route.ts`)
* Built dashboard page with 4 KPI stat cards (`src/app/[locale]/(dashboard)/page.tsx`)
* Created placeholder pages with empty states for: Customers, Projects, Time Tracking, Invoices
* Middleware combines next-intl locale routing + JWT auth protection (`src/middleware.ts`)
* Multi-tenant helpers: `getTenantId()`, `requireAuth()` in `src/lib/tenant.ts`
* Seed script with demo tenant + user (`prisma/seed.ts`): `demo@miniams.dev` / `password123`
* Full translation files for DE and EN (`messages/en.json`, `messages/de.json`)

## 3. The "Pain" Log (CRITICAL)
* **Tried:** Prisma 6 new `prisma-client` generator with custom output to `src/generated/prisma`
* **Failed:** Next.js Turbopack couldn't resolve `@/generated/prisma` — module not found error during build
* **Workaround:** Switched to classic `prisma-client-js` generator (no custom output), import from `@prisma/client`
* *Note:* Do not use `provider = "prisma-client"` with custom output in this project. Stick with `prisma-client-js`.

* **Tried:** `npx create-next-app miniAMS-temp` (capital letters)
* **Failed:** npm naming restrictions reject capital letters
* **Workaround:** Used `miniams-temp` then renamed in package.json

## 4. Active Variable State
* `.env`: DATABASE_URL=`postgresql://postgres:postgres@localhost:5432/miniams?schema=public`, AUTH_SECRET=`dev-secret-change-me-in-production`, AUTH_URL=`http://localhost:3000`
* Port 3000 was in use during dev test — Next.js auto-selected port 3003
* Prisma schema uses `prisma-client-js` provider (classic), no custom output
* `prisma.config.ts` has seed command configured: `tsx prisma/seed.ts`
* Next.js 16.1.6 shows deprecation warning: "middleware" file convention deprecated in favor of "proxy" — can ignore for now
* Turbopack workspace root warning due to parent lockfile at `/Users/oliverbaer/package-lock.json`

## 5. Immediate Next Steps
1. [ ] Phase 2: CRM Module — Customer CRUD (list, detail, create/edit forms, search/filter)
2. [ ] Phase 2: Contacts — inline management on Customer detail page
3. [ ] Phase 2: Projects — CRUD with customer linking, team assignment, budget tracking
4. [ ] Phase 3: Time Tracking — timer mode, manual entry, weekly overview, approval workflow
5. [ ] Phase 4: Invoicing — create from time entries/services, PDF generation, status workflow
6. [ ] Phase 5: Dashboard with real data, settings pages, final polish
7. [ ] Need PostgreSQL running locally to test full auth flow (register → login → dashboard)
8. [ ] Consider migrating middleware.ts to proxy pattern (Next.js 16 deprecation)
